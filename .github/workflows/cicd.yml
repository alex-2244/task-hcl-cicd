name: TestWorkflow

on:
    pull_request:
        branches:
            - main
        branches-ignore:
            - develop

jobs:
    build:
        runs-on: ubuntu-latest

        steps:
            - name: print
              run: echo "Welcome to Github Actions!!"

    test:
        runs-on: ubuntu-latest

        steps:
            - name: Display
              run: echo "My testing workflow"
